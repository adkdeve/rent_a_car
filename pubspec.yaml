name: rent_a_car_project
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  smooth_page_indicator: ^1.0.0+2
  cupertino_icons: ^1.0.8
  flashy_tab_bar2: ^0.0.7
  path_provider: ^2.0.11
  geolocator: ^9.0.2
  geocoding: ^2.0.5
  reveal_on_scroll: ^0.0.3
  flutter_onboarding_slider: ^1.0.11
  shared_preferences: ^2.3.5
  flutter_svg: ^2.0.4 # or the latest version
  firebase_core: ^3.10.0
  lottie: ^1.2.1
  firebase_auth: ^5.4.0
  provider: ^6.1.2
  cloud_firestore: ^5.6.1
  image_picker: ^1.1.2
  fade_shimmer: ^2.3.0
  badges: ^3.1.2
  flutter_staggered_animations: ^1.1.1
  pull_to_refresh: ^2.0.0




  swipeable_card_stack: any
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Adding the assets section
  assets:
    - assets/avatar.jpg
    - assets/category_default.png
    - assets/success_checkmark.json
